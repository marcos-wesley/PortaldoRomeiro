# Production environment variables for Portal do Romeiro
# Fill values and set them in your hosting environment (do not commit secrets).

# Backend (server)
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/portal_romeiro
SESSION_SECRET=change_this_long_random_secret
NODE_ENV=production
PORT=5000
ADMIN_EMAIL=admin@yourdomain.com
ADMIN_PASSWORD=strong_password_here

# CORS allowlist (hostnames only, comma-separated)
REPLIT_DOMAINS=api.yourdomain.com,app.yourdomain.com

# Frontend (Expo public)
# Use your public API domain (no port in production)
EXPO_PUBLIC_DOMAIN=api.yourdomain.com
EXPO_PUBLIC_SCHEME=https

# Payments (optional if managed via admin settings)
MERCADOPAGO_ACCESS_TOKEN=APP_USR-your-token
MERCADOPAGO_PUBLIC_KEY=APP_USR-your-public-key
